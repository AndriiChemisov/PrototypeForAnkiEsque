<UserControl x:Class="PrototypeForAnkiEsque.Views.FlashcardDeckCreatorUserControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrototypeForAnkiEsque.Views"
        xmlns:converters="clr-namespace:PrototypeForAnkiEsque.Converters"
        xmlns:viewModels="clr-namespace:PrototypeForAnkiEsque.ViewModels"
        mc:Ignorable="d"
        Height="450" Width="800">

    <UserControl.Resources>
        <converters:EaseRatingToStringConverter x:Key="EaseRatingToStringConverter"/>
    </UserControl.Resources>

    <Grid>
        <Button Content="Main Menu" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20" Command="{Binding OpenMainMenuCommand}"/>

        <StackPanel VerticalAlignment="Top" 
                           HorizontalAlignment="Center"
                           Width="200">
            <TextBlock Text="Enter deck name" HorizontalAlignment="Center" FontSize="16" Margin="10"/>

            <TextBox Text="{Binding DeckName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
        </StackPanel>

        <TextBlock Text="Select Flashcards" HorizontalAlignment="Center" FontSize="16" Margin="0,74,0,-54"/>

        <DataGrid x:Name="FlashcardDataGrid" 
                      AutoGenerateColumns="False"  
                      CanUserAddRows="false"
                      IsReadOnly="True" Margin="0,79,0,71"
                      Height="250"
                      ItemsSource="{Binding Flashcards}"
                      SelectionMode="Extended">
            <!-- Define column for Front property -->
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Select" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox Command="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, 
                                        Path=DataContext.ToggleFlashcardSelectionCommand}"
                      CommandParameter="{Binding Id}"
                      IsChecked="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, 
                                         Path=DataContext.SelectedFlashcards[Id], 
                                         Mode=TwoWay}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Front" Binding="{Binding Front}" Width="*" />
                <DataGridTextColumn Header="Back" Binding="{Binding Back}" Width="*" />
                <DataGridTemplateColumn Header="Rating" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding EaseRating, Converter={StaticResource EaseRatingToStringConverter}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <!-- You can add more columns as necessary -->
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="Save Deck" Command="{Binding CreateDeckCommand}" HorizontalAlignment="Center" VerticalAlignment="Top" Width="100" Margin="0,395,0,0"/>
    </Grid>
</UserControl>
